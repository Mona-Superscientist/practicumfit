const invalidCredentials = 'Incorrect username or password.';
const missingRequiredField = 'Please enter an email and password';
const nonExistingUser = 'User does not exist.';

export {
    invalidCredentials,
    missingRequiredField,
    nonExistingUser,
}
